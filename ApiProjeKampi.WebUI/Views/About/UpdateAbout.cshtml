@model GetAboutByIdDto
@{
    ViewData["Title"] = "UpdateAbout";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<section class="section">
    <div class="section-body">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h4>Hakkımda Güncelle</h4>
                    </div>
                    <div class="card-body">
                        <form method="post">
                            <div class="form-group">
                                <input type="hidden" asp-for="AboutId" />

                                <label>Ana Başlık</label>
                                <input type="text" asp-for="Title" class="form-control" placeholder="Ana başlığı giriniz..." required>

                                <label class="mt-2">Açıklama</label>
                                <textarea asp-for="Description" class="form-control" placeholder="Kısa açıklama giriniz..." rows="3" required></textarea>

                                <label class="mt-2">Video URL</label>
                                <input type="text" asp-for="VideoUrl" class="form-control" placeholder="Video bağlantısını giriniz..." required>

                                <label class="mt-2">Video Cover Görsel URL</label>
                                <input type="text" asp-for="VideoCoverImageUrl" class="form-control" placeholder="Video Cover Görsel bağlantısını giriniz..." required>

                                <label class="mt-2">Görsel URL</label>
                                <input type="text" asp-for="ImageUrl" class="form-control" placeholder="Görsel bağlantısını giriniz..." required>


                                <label class="mt-2">Rezervasyon Numarası</label>
                                <input type="text" asp-for="ReservationNumber" class="form-control" placeholder="Rezervasyon Numarasını giriniz..." required>
                            </div>

                            <div class="form-group">
                                <button type="submit" class="btn btn-primary">
                                    <i class="fas fa-save"></i> Değişiklikleri Kaydet
                                </button>
                                <a href="/About/AboutList/" class="btn btn-secondary">
                                    Vazgeç / Listeye Dön
                                </a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

