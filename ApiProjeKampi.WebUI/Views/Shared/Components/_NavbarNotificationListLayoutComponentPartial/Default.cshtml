@model List<NotificationListByIsReadFalseDto>

<li class="dropdown dropdown-list-toggle">
    <a href="#" data-toggle="dropdown"
       class="nav-link notification-toggle nav-link-lg">
        <i data-feather="bell" class="bell"></i>
        <span class="badge headerBadgeColor1">
            @Model.Count
        </span>
    </a>
    <div class="dropdown-menu dropdown-list dropdown-menu-right pullDown">
        <div class="dropdown-header">
            Bildirimler
            <div class="float-right">
                <a href="#">Tümünü okundu işaretle</a>
            </div>
        </div>
        <div class="dropdown-list-content dropdown-list-icons">
            @foreach (var item in Model)
            {
            <a href="#" class="dropdown-item dropdown-item-unread">
                <span class="dropdown-item-icon bg-primary text-white">
                    <i class="@item.IconUrl"></i>
                </span> <span class="dropdown-item-desc">
                    @item.Description
                     <span class="time">
                            @item.NotificationDate.Date.ToString("dd/MM/yyyy HH:mm")
                    </span>
                </span>
               </a>
            }
        </div>
        <div class="dropdown-footer text-center">
            <a href="/Notification/NotificationList/">Tümünü gör <i class="fas fa-chevron-right"></i></a>
        </div>
    </div>
</li>